<h2 class="border">Projects</h2>

<!-- Projects -->
<div class="items items-images">
  <% if !@projects.blank? %>
  <ul class="projects">
    <% for project in @projects %>
      <li>
        <div class="cl">&nbsp;</div>
        <div class="item-image">
          <%= link_to image_tag(project.image, :alt => project.title), project_path(project), :class => 'aimg' %>
        </div>
        <div class="item-info">
          <small><a href="projects.html">Gnomeslab</a> / <%= project.date %></small>
          <h3>
            <%= link_to project.title, project_path(project) %>
          </h3>
          <p><%= project.subtitle %></p>
          <p class="view"><%= link_to 'View case study', project_path(project) %></p>
        </div>
        <div class="cl">&nbsp;</div>
      </li>
    <% end %>
  </ul>
  <% end %>
</div>
<!-- End Projects -->

<!-- Pagination -->
<%= will_paginate @projects,
                  :previous_label => '&lt; Prev',
                  :next_label => 'Next &gt;',
                  :separator => '<span>|</span>' %>
<!-- End Pagination -->

<%= content_for :sidebar do %>
  <%= render 'categories_list' %>
  <%= render 'contact_teaser' %>
<% end %>